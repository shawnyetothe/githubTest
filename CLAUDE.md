# CLAUDE.md

This file provides guidance for AI assistants working with the githubTest repository.

## Repository Overview

This is a newly initialized repository with minimal structure. It currently serves as a test/starter project.

## Project Structure

```
githubTest/
├── README.md        # Project readme (title only)
└── CLAUDE.md        # This file — AI assistant guidance
```

## Development Setup

No build system, package manager, or dependencies are configured yet. When tooling is added, update this section with:

- Language and runtime version requirements
- Install commands (e.g., `npm install`, `pip install -r requirements.txt`)
- Environment variable setup

## Build and Run

No build or run commands are configured. Update this section when a build system is added.

## Testing

No test framework is configured. Update this section when tests are added.

## Linting and Formatting

No linting or formatting tools are configured. Update this section when they are added.

## Git Conventions

- **Default branch:** `master`
- **Remote:** `origin`
- Write clear, descriptive commit messages
- Keep commits focused on a single logical change

## Code Conventions

No code exists yet. When code is added, document conventions here including:

- Code style and formatting rules
- Naming conventions
- File and directory organization patterns
- Error handling patterns
